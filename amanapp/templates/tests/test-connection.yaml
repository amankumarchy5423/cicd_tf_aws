apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amanapp.fullname" . }}-test-connection"
  labels:
    {{- include "amanapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amanapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
