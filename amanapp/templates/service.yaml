apiVersion: v1
kind: Service
metadata:
  name: {{ include "amanapp.fullname" . }}
  labels:
    {{- include "amanapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "amanapp.selectorLabels" . | nindent 4 }}
